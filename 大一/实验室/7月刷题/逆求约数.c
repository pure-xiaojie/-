#include "stdio.h"
int main()
{
    int n,a,b,c;
    scanf("%d",&n);
    while(n--)
    {
        scanf("%d%d",&a,&b);
        c+=b*2;
       while()
            if(a%c == b)
        {
            printf("%d\n",c);
            break;
        }
    }
    return 0;
}

